= javascript_include_tag "https://ajax.googleapis.com/ajax/libs/swfobject/2.2/swfobject.js", "video.js"
= stylesheet_link_tag "video"

:javascript
  $(document).ready(function() {
    createRebeccas(#{@rebeccas},#{@width},#{@height});
    window.setInterval(moveRebecca,500);
  });

- @rebeccas.to_i.times do |i|
  %div{:id => "playerContainer#{i}", :class => "playerContainer", :width => @width, :height => @height}
    %div{:id => "playerArea#{i}"}
      You don't have the right version of flash
